# Audio samples

Drop the rendered demo clips here, referenced by the "Hear it" section on the homepage (`site/index.md`):

- `default.wav` — default tone (Mesa Dual Rectifier · Mesa V30 · Tube Screamer, stock knob positions)
- `clean_melodic.wav` — `clean_melodic.toml` preset
- `solo_seeker.wav` — `solo_seeker.toml` preset

Suggested source: run the same DI guitar track through each bundled preset (e.g. via `examples/di_compare.rs` or the app's recorder) and trim to ~10-15s.

A 4th tile is left as a "more coming soon" placeholder in `site/index.md` for the next clip.
